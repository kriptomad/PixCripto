# URL base da API FastAPI do PixCripto (backend).
# Em desenvolvimento local, o padrao (http://127.0.0.1:8000) ja funciona
# desde que o backend esteja rodando com CORS habilitado (PIXCRIPTO_CORS_ORIGINS).
VITE_API_BASE_URL=http://127.0.0.1:8000
